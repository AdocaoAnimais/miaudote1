package com.projeto2.miaudote.application.handler

/**
 * Interface que representa um manipulador de requisição.
 *
 * Esta interface serve como base para todos os manipuladores de requisição, que devem implementar a lógica
 * necessária para processar os dados recebidos.
 */
interface  RequestHandler