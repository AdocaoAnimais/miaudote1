CREATE TABLE IF NOT EXISTS USUARIO
(
    USUARIO_ID       INTEGER      NOT NULL PRIMARY KEY AUTOINCREMENT,
    USERNAME         VARCHAR(100) NOT NULL,
    NOME             VARCHAR(60)  NOT NULL,
    SOBRENOME        VARCHAR(60)  NOT NULL,
    CPF              VARCHAR(11)  NOT NULL,
    EMAIL            VARCHAR(50)  NOT NULL,
    SENHA            VARCHAR(60)  NOT NULL,
    ENDERECO_ID      INTEGER,
    CONTATO          VARCHAR(15),
    PERFIL_ACESSO    CHAR(1),
    FOTO             BLOB,
    DESCRICAO        VARCHAR(240),
    EMAIL_VERIFICADO BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (ENDERECO_ID) REFERENCES ENDERECO (ENDERECO_ID)
);

INSERT INTO USUARIO (USERNAME, NOME, SOBRENOME, CPF, EMAIL, SENHA, ENDERECO_ID, CONTATO, PERFIL_ACESSO, FOTO, DESCRICAO,
                     EMAIL_VERIFICADO)
VALUES ('usuario1', 'João', 'Silva', '12345678901', 'joao@example.com', 'senha123', 1, '123456789', 'U', NULL,
        'Descrição do usuário 1', FALSE);

INSERT INTO USUARIO (USERNAME, NOME, SOBRENOME, CPF, EMAIL, SENHA, ENDERECO_ID, CONTATO, PERFIL_ACESSO, FOTO, DESCRICAO,
                     EMAIL_VERIFICADO)
VALUES ('usuario2', 'Maria', 'Santos', '98765432109', 'maria@example.com', 'senha456', 2, '987654321', 'U', NULL,
        'Descrição do usuário 2', TRUE);
