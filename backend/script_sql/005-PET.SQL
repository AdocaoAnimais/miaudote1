CREATE TABLE IF NOT EXISTS PET
(
    PET_ID              INTEGER     NOT NULL PRIMARY KEY AUTOINCREMENT,
    NOME                VARCHAR(50) NOT NULL,
    SEXO                CHAR(1)     NOT NULL,
    IDADE               INTEGER,
    PORTE               CHAR(1),
    CASTRADO            CHAR(1),
    DATA_CADASTRO       DATE        NOT NULL DEFAULT CURRENT_TIMESTAMP,
    DESCRICAO           VARCHAR(240),
    FOTO                BLOB,
    USUARIO_CADASTRO_ID INTEGER     NOT NULL,
    FOREIGN KEY (USUARIO_CADASTRO_ID) REFERENCES USUARIO (USUARIO_ID)
);

INSERT INTO PET (NOME, SEXO, IDADE, PORTE, CASTRADO, DATA_CADASTRO, DESCRICAO, FOTO, USUARIO_CADASTRO_ID)
VALUES ('Rex', 'M', 3, 'P', 'S', CURRENT_DATE, 'Descrição do pet Rex', NULL, 1);

INSERT INTO PET (NOME, SEXO, IDADE, PORTE, CASTRADO, DATA_CADASTRO, DESCRICAO, FOTO, USUARIO_CADASTRO_ID)
VALUES ('Luna', 'F', 2, 'M', 'N', CURRENT_DATE, 'Descrição do pet Luna', NULL, 2);

